# walk-app テストケース

最終更新: 2026-08-08

要件は [requirements.md](./requirements.md)、設計は [design.md](./design.md)、作業手順は [development-plan.md](./development-plan.md) を参照。

## 方針

**テスト駆動で進める。** 実装より先にテストを書き、落ちることを確かめてから実装する。

| 項目 | 内容 |
|---|---|
| 実行 | `npm test`（監視は `npm run test:watch`） |
| 構成 | `jest-expo` プリセット |
| 置き場所 | `__tests__/<モジュール名>-test.ts` |

この表の「ケース」は、テストコード中の `it(...)` の文言とそのまま対応させる。
片方だけ変えないこと。

**照合はテストで自動化してある**（`__tests__/test-cases-doc-test.ts`）。
md に載っていない `it(...)` があるとテストが落ちる。

| ケース | 目的 |
|---|---|
| すべてのテストケースが md に載っている | 記載漏れを機械的に捕まえる |
| テストケースを 1 件以上見つけられている | 正規表現が壊れて 0 件になり、素通りするのを防ぐ |

## 何をテストで押さえ、何を実機で見るか

`lib/` は純粋なロジックなので、実機なしで確かめられる。
実機が要るのは、地図の描画・GPS・実際に歩いた結果だけ。

| 対象 | 確認方法 |
|---|---|
| タイル計算、コストモデル、グラフ構築、経路探索 | **自動テスト** |
| Overpass からの取得（通信あり） | 実機。取得数を[実測値](#実測値との突き合わせ)と突き合わせる |
| 地図表示、現在地、ルート描画 | 実機（室内） |
| 幹線回避が体感に合うか、多様性、バッテリー | 実機（外を歩く）。[動作確認の場所](./development-plan.md#動作確認の場所) |

---

## lib/tiles.ts — タイル計算

`__tests__/tiles-test.ts`

### tileAt

| ケース | 目的 |
|---|---|
| 開発エリアが実測したタイルと一致する | 実測に使った bbox とズレていないことの固定 |
| タイル境界ちょうどの座標が、そのタイルの内側に入る | **回帰テスト。** 度のまま割ると `139.70 / 0.02 = 6984.999999999999` となり、境界の座標が隣のタイルに落ちる |
| 任意の座標が、自分のタイルの範囲内に収まる | 2 万点の走査。境界の取りこぼしが無いこと |
| 小数 7 桁より細かい差は無視して上のタイルに入れる | OSM の座標精度に丸める仕様の明示 |
| 負の座標でも下側に丸める | 赤道・本初子午線をまたぐ側の挙動 |

### tileBounds

| ケース | 目的 |
|---|---|
| 誤差のない値を返す | Overpass の bbox 文字列が汚れないこと |
| 1 辺が TILE_SIZE になる | [タイルの定義](./design.md#21-タイルの定義)どおりの大きさ |
| 隣接タイルが隙間なく接する | 取りこぼし・重複が出ないこと |

### tileKey

| ケース | 目的 |
|---|---|
| 往復して元に戻る | DB のキーに使うため |

### tilesAround

関東全域を入れると保存済みタイルは 8,000 枚を超える。
**読む範囲は常にここで絞る**（全部まとめて読むとメモリに載らない）。

| ケース | 目的 |
|---|---|
| 半径 0 なら中心の 1 枚だけ | |
| 半径 1 なら 3×3 の 9 枚 | [規模の見積り](./design.md#24-規模の見積り)の「3×3 タイル」に対応する単位 |
| 中心を必ず含む | 現在地のタイルが落ちないこと |
| 同じタイルを二度返さない | 重複して読まないこと |

---

## lib/cost.ts — コストモデル

`__tests__/cost-test.ts`

| ケース | 目的 |
|---|---|
| 設計表の値を返す | [種別係数](./design.md#13-コストモデル)と実装のズレを防ぐ |
| 歩行者専用道が生活道路より安く、幹線が高い | **大小関係そのものが要件。** 値を調整しても壊れてはいけない順序 |
| 接続路は接続元と同じ係数になる | 実データにある `primary_link` などの扱い |
| 表に無い種別は 1.0 になる | `cycleway`・`rest_area`・`corridor` など、設計表で網羅できていない種別 |
| 接続元も未知なら 1.0 に落ちる | フォールバックの二段目 |
| タグが無ければ補正しない | `lanes` は日本の生活道路にほぼ無い（[前提調査](./design.md#11-前提調査-lanes-タグは使えない)） |
| 2 車線以上は 1.5 倍 / 1 車線以下は 0.9 倍 | 補正の値 |
| 解釈できない値では補正しない | `lanes=unknown` のような実データ |
| 複数値や小数でも先頭の数値で判断する | `lanes=2;3` のような実データ |
| 種別係数と車線補正を掛け合わせる | `primary` かつ 2 車線 → 12.0 |
| 車線タグが無ければ種別係数そのまま | 補正が勝手に掛からないこと |
| highway が無くても落ちない | タグ欠損への耐性 |

### edgeFactor — footway の種別

関東全域の実測で `highway=footway` の **6 割が歩道と横断歩道**だった
（[footway の 6 割は道路の付属物](./design.md#111-footway-の-6-割は道路の付属物未解決)）。
車道に付随する線を 0.8 で優遇すると、**大通り沿いの歩道が最も安い道になり**、
「幹線を避ける」が骨抜きになる。

| ケース | 目的 |
|---|---|
| 歩道は優遇しない | `footway=sidewalk` は 1.5。日本では歩道が別に描かれる道路がおおむね `tertiary` 以上なので、幹線沿いの proxy として基準より不利にする |
| 横断歩道は基準と同じにする | `footway=crossing` は 1.0。横断は避けようがないので、優遇だけ外す |
| 種別の無い footway は歩行者専用道として優遇する | 公園内の道など。0.8 のまま |
| 知らない footway 種別なら優遇したままにする | `access_aisle` や `traffic_island`。歩道と決めつけない |
| footway 以外の種別では footway タグを見ない | 実データには `highway=path` に `footway=sidewalk` が付いた例がある |
| 歩道でも車線補正は掛かる | 種別係数と車線補正が独立していること |

**値そのものは[未決事項](./requirements.md#7-未決事項)。** 実際に歩いて調整するため、
テストは「設計表と一致すること」と「大小関係」を押さえ、値の妥当性は問わない。

---

## lib/overpass.ts — 道路網の取得

`__tests__/overpass-test.ts`

通信そのものはテストしない。`fetchImpl` を差し替えて、
**クエリの組み立て・応答の解釈・エンドポイントの切り替え**を確かめる。

| ケース | 目的 |
|---|---|
| フォールバック先を複数持つ | 公開サーバーは過負荷で 504 を返す。実測で overpass-api.de 17 秒に対し kumi.systems 3 秒 |
| way と node に分けて返す | 応答の解釈 |
| タグの無い way でも落ちない | データ欠損への耐性 |
| タイルの bbox をクエリに含める | [タイルの定義](./design.md#21-タイルの定義)どおりの範囲を要求すること |
| ノード ID が返る取得形式を使う | **`out geom` に戻すと隣接タイルを ID で繋げなくなる**（[タイル間の接続](./design.md#23-タイル間の接続)） |
| 1 つ目が成功すれば 2 つ目は叩かない | 公共サービスに余計な負荷をかけないこと |
| 504 が返ったら次のエンドポイントを試す | 過負荷時の回復 |
| 通信そのものが失敗しても次を試す | 圏外・切断からの回復 |
| 空の応答なら次のエンドポイントを試す | **地域限定インスタンスは範囲外に 200 と 0 件を返す**（実測: overpass.osm.ch）。道が無いタイルとして保存されるのを防ぐ |
| どこも空を返すなら、空のまま受け入れる | 海上や山奥には本当に道が無いタイルがある |
| 全滅したら、試した結果が分かるエラーを投げる | どのエンドポイントがどう失敗したか分かること |
| 種別ごとに数え、多い順に並べる | 取得結果の目視確認用 |
| highway が無い way も数える | 取りこぼさないこと |

---

## lib/graph.ts — グラフ構築

`__tests__/graph-test.ts`

### distanceMeters

| ケース | 目的 |
|---|---|
| 同じ点は 0 | |
| 既知の距離とおおよそ合う | 東京駅–横浜駅 約 27km |
| 緯度 0.001 度はおよそ 111m | 桁を間違えていないこと |
| 向きを変えても同じ距離 | 対称性 |

### buildGraph

| ケース | 目的 |
|---|---|
| 交わらない 1 本の way は 1 エッジになる | 過剰な分割をしないこと |
| 途中の形状点をエッジの geometry に残す | 地図に描くとき道の形が保たれること |
| 他の way と共有するノードで分割する | **ここが肝。** 間違えると経路が繋がらない |
| way の両端は共有されていなくてもノードになる | 行き止まりと、隣接タイルとの接続点 |
| 種別ごとの係数をエッジに持たせる | 探索が係数を使えること |
| 孤立したノードを作らない | 到達不能な断片が生じないこと |
| 環状の way を弧に分ける | ロータリーや公園の周回路。他の道が繋がるノードで切り、長さ 0 のループを作らない |
| どこにも繋がらない環状路は落とす | 切る場所が無く、経路としても到達できない。落として構わない |
| 座標が欠けた way は無視する | データ欠損への耐性 |
| ノードが 1 つしかない way は無視する | 同上 |
| 空の入力でも落ちない | 同上 |

### neighbors

| ケース | 目的 |
|---|---|
| 両方向に辿れる | 徒歩なのでエッジは無向 |
| 交差点から全ての方向を列挙する | 探索が分岐を見落とさないこと |
| 知らないノードでは空を返す | 例外を投げないこと |

### reachableFrom / areConnected / largestComponentSize

**Step 1-5 の完了の定義**（隣接タイルの端から端まで辿れる）を確かめるための道具。
Step 2 の経路探索も同じ隣接リストを辿る。

| ケース | 目的 |
|---|---|
| 繋がっているノードをすべて返す | 探索の土台 |
| 出発点そのものを含む | |
| 分断された先には届かない | 到達判定が甘くないこと |
| 知らないノードからは空を返す | 例外を投げないこと |
| 環状でも無限に回らない | 訪問済みを覚えていること |
| 繋がっていれば true / 繋がっていなければ false | |
| **隣接タイル由来の way が 1 本に繋がる** | **1-5 の肝。** way ID で重複排除したあと、タイル A の端からタイル B の端まで辿れる |
| 境界の node が片方のタイルにしか無くても繋がる | `loadTiles` が両方から node を集めていること |
| 最大の連結成分の大きさを返す | 分断の検出。全ノード数と離れていればどこかで切れている |
| 空のグラフでは 0 | |
| 全て繋がっていればノード数と一致する | |

### nearestNode

現在地も、地図をタップした目的地も道の上には無い。探索の両端を
グラフに載せるために使う。

| ケース | 目的 |
|---|---|
| 一番近いノードを返す | |
| ノードと同じ座標ならそのノードを返す | |
| 遠く離れた点でも一番近いノードを返す | 距離の上限は設けない |
| 空のグラフでは undefined を返す | 未投入の状態 |

### edgeNode

タイル内で最も西／東にあるノードを選ぶ。境界のすぐ近くではなく**端どうし**を
選ぶことで、境界をまたいで実際にグラフが繋がっていることを確かめられる。

| ケース | 目的 |
|---|---|
| タイル内で最も西のノードを返す | 1-5 の出発点 |
| タイル内で最も東のノードを返す | 1-5 の到達点 |
| タイルの外にあるノードは選ばない | **Overpass は bbox の外まで返す。** 隣のタイルのノードを掴まないこと |
| タイル内にノードが無ければ undefined | 未取得のタイルを指した場合 |
| 空のグラフでも落ちない | 起動直後 |

---

## lib/db.ts — タイルの保存

`__tests__/db-test.ts`

**実際の SQLite に対してテストする。** `node:sqlite`（Node 22 に同梱）を
`expo-sqlite` と同じ形の口で包み、SQL そのものを確かめる。
実機では `lib/store.ts` が `expo-sqlite` を渡す。

保存するのは Overpass が返した生の way / node で、グラフではない。
**グラフは読み込んだタイル全体から組み立てる**必要があるため
（あるノードが交差点かどうかは隣のタイルを読んだ時点で変わる）、
タイルごとにエッジを作って保存すると辻褄が合わなくなる。

| ケース | 目的 |
|---|---|
| 何度呼んでも落ちない | 起動のたびに `initSchema` を通すため |
| 同じスキーマなら保存済みのデータを残す | 起動のたびに消さないこと |
| 古いスキーマの DB は作り直す | **回帰テスト。** 列を変えたのに古い表が残り「no such column: w.highway」になっていた |
| バージョンが記録されていない古い DB も作り直す | **回帰テスト。** 0 を「まだ何も無い」とみなして作り直しを飛ばしていた |
| 版数が合っていても、列が足りない表は作り直す | **回帰テスト。** `PRAGMA user_version` を信用せず実際の表の形を見る |
| 使わなくなった表を残さない | `node_tiles` の取り残しを防ぐ |
| タイルから way を引くのに ways を全走査しない | **回帰テスト。** `way_tiles` の主キーは `(way_id, x, y)` で先頭が way_id のため、タイル側から引く索引が別に要る。無いと `loadTiles` が毎回 `ways` を頭から舐める（関東全域で 236 万行）。[探索 2 秒](./requirements.md#5-非機能要件)を守れない |
| 保存したタイルを取得済みと判定する | 通信するかどうかの判断 |
| 取得済みタイルを一覧できる | 何が手元にあるかの確認手段 |
| 同じタイルを二度保存しても重複しない | 再取得しても増えないこと |
| 保存した way と node をそのまま返す | タグと node 参照が往復すること |
| 使うタグだけを保存する | `highway` と `lanes` 以外は保存しない。1 タイル 784KB → 404KB の内訳の 1 つ |
| lanes が無い way でも往復する | タグ欠損 |
| 座標が丸められずにそのまま往復する | **精度を落とさないこと。** 整数化すれば縮むが採らなかった |
| ノードを多く持つ way でも往復する | 長さと順序 |
| OSM の大きな ID でも往復する | ノード ID は 100 億を超える |
| 未取得のタイルを指定しても落ちない | 初回起動 |
| タイルを指定しなければ空を返す | 空配列で SQL を組まないこと |
| 複数タイルをまとめて読める | 3×3 の読み込み |
| 境界をまたぐ way は、どちらのタイルからも読める | Overpass は bbox 外まで返す（[タイル間の接続](./design.md#23-タイル間の接続)） |
| 両方のタイルに含まれる way を二重に返さない | OSM ID で重複排除できていること |
| way が参照する node は、別タイル由来でも揃う | **境界の way が欠けたノードを指さないこと。** 欠けるとその way が捨てられ、タイルが繋がらない |
| footway の種別が往復する | 歩道・横断歩道を区別できないと、コストモデルが大通り沿いの歩道を優遇してしまう（[footway の 6 割は道路の付属物](./design.md#111-footway-の-6-割は道路の付属物未解決)） |
| footway タグが無い way でも往復する | タグ欠損。列を足しても既存の way が壊れないこと |

---

## lib/probe.ts — 大きな DB の検証（Step 1-4）

`__tests__/probe-test.ts`

[F-10](./requirements.md#f-10-オフラインデータの一括投入) が置く約 900MB を
Expo Go が扱えるか確かめるための使い捨ての DB。
中身に意味は無いので `randomblob` で 1MB ずつ膨らませる。

| ケース | 目的 |
|---|---|
| 空の DB でもサイズを返す | `PRAGMA page_count` × `page_size` で測れること |
| 行を足すと増える | サイズ計算が実態を追うこと |
| 目標サイズまで膨らませる | 900MB に届くこと |
| すでに目標に達していれば何も足さない | 押すたびに膨らみ続けないこと |
| 進捗を報告する | 実機で数分かかるため、進み具合が見えること |
| 中断できる | **途中でやめられること。** 端末の容量を守る |
| 行数と、読み出したデータのサイズを返す | 書けるだけでなく読めること |
| 空でも落ちない | 未生成の状態 |

**実機で確かめること:** 900MB に到達するか、クエリが返るか、削除できるか。
テストでは小さい目標値（数 MB）で論理だけを押さえている。

### lib/probe-store.ts — 開閉と削除の順序

`__tests__/probe-store-test.ts`

**expo-sqlite の削除は当てにしない。** 開いたままの接続があると失敗し、
例外を投げずに成功したように見えてもファイルが残ることがある。
接続を閉じて削除を試みたあと、**ディスクを見て残っていたら直接消す**。
フェイク側でこれらの挙動を再現して確かめる。

| ケース | 目的 |
|---|---|
| 二度開いても接続を使い回す | 接続が増え続けないこと |
| 削除する前に接続を閉じる | **回帰テスト。** 閉じずに削除して「Calling the deleteDatabaseAsync function has failed」になっていた |
| 開いていなくても削除できる | 一度も開かずに押した場合 |
| expo-sqlite が消せなかったら、ファイルを直接消す | **回帰テスト。** 例外は出ないのに 900MB が残り続けた |
| deleteDatabaseAsync が例外を投げても、ファイルを直接消して回復する | 例外で止まらないこと |
| WAL の付随ファイルも一緒に消す | `-wal` / `-shm` の取り残しを防ぐ。他の DB は消さない |
| ファイル削除が例外を投げても、残りを続けて報告する | 1 つ失敗しても他を消す。何が失敗したかを出す |
| それでも消えなければ、残っているものを報告する | 黙って失敗しないこと |
| 削除したあとに開くと、新しい接続になる | 消えた DB の接続を掴み続けないこと |
| 閉じたあとに閉じても落ちない | 二重呼び出し |
| 開いていない状態で閉じても落ちない | 同上 |
| 削除に失敗したら、接続を持ち越さない | 失敗後もずっと失敗し続けないこと |

---

## lib/osm-filter.ts — 抽出とタイル振り分け

`__tests__/osm-filter-test.ts`

**アプリと前処理スクリプトが共有する規則。** 別々に書くと、Step 1 の実測値と
Step 3 で生成した DB が食い違ってしまう。

| ケース | 目的 |
|---|---|
| 生活道路や歩行者専用道は通す | 歩ける道を落とさないこと |
| 歩行者が入れない道を落とす | `motorway` / `trunk` とその `_link` |
| 徒歩を禁じている道を落とす | `foot=no` |
| 私有地を落とす | `access=private` |
| 工事中や計画中を落とす | `construction` / `proposed` |
| highway が無いものを落とす | 建物や土地利用など、道ではない way |
| 知らない種別は通す | `cycleway` など。係数は 1.0 に落ちる |
| foot が no 以外なら落とさない | `foot=yes` / `designated` を巻き添えにしない |
| 1 つのタイルに収まる way は 1 枚だけ返す | 過剰に振り分けない |
| **境界をまたぐ way は両方のタイルを返す** | **タイル接続の肝。** 構成ノードが入るタイルすべてに入れる |
| 同じタイルを重複して返さない | |
| 斜めに長い way は 4 枚返すこともある | 対角にまたぐ場合 |
| 空なら空を返す | |

---

## scripts/build-db.ts — PBF から DB を作る

`__tests__/build-db-script-test.ts`

**中身ではなく、起動できることだけを見る。** 抽出規則は
[lib/osm-filter.ts](#libosm-filterts--抽出とタイル振り分け) 側でテストしてある。
ここが防ぐのは、461MB を読ませて数十分待った末に「そもそも起動しなかった」という事故。

| ケース | 目的 |
|---|---|
| 引数が無ければ使い方を出して終わる | 起動して引数を検査するところまで到達すること |
| 変換に失敗せずに読み込める | **回帰テスト。** トップレベル `await` を書いたら tsx の CJS 変換が落ち、PBF を渡す前に死んでいた。引数不足も変換失敗も終了コードは 1 なので、中身で見分ける |

---

## lib/transfer.ts — 端末への投入の判断

`__tests__/transfer-test.ts`

通信そのものは画面側が `expo-file-system` で行う。ここには**判断だけ**を置いて、
実機なしで確かめられるようにしてある。

### checkSpace

| ケース | 目的 |
|---|---|
| 空きが足りていれば通す | |
| 空きが足りなければ止める | [F-10](./requirements.md#f-10-オフラインデータの一括投入) の「足りなければ開始しない」 |
| 足りないときは不足分を返す | あと何 MB 空ければよいか出せること |
| ちょうどの空きは足りないとみなす | 端末を満杯にしない。余白を残す |
| 余白のぶんまで空いていれば通す | 余白の境目 |
| 入れ替えなら古い DB のぶんを空きに数える | 年 1 回の地図更新。先に消してから入れる |
| 大きさが分からなければ止める | HEAD が Content-Length を返さない場合。当て推量で始めない |

### dbUrl

| ケース | 目的 |
|---|---|
| ホストとポートから URL を組む | |
| ホストに書かれた余分な空白を落とす | 手で打ち込むため |
| すでに URL の形なら、そのまま使う | まるごと貼られた場合 |

### databaseFileUri

| ケース | 目的 |
|---|---|
| file:// が無ければ付ける | `expo-sqlite` の `defaultDatabaseDirectory` が素のパスで来る場合。進捗を出すために使う legacy の `createDownloadResumable` は `file://` URI しか受けない |
| file:// が付いていればそのまま使う | 環境によってどちらでも来る |
| 末尾のスラッシュが重ならない | |

---

## lib/route.ts — 経路探索

`__tests__/route-test.ts`

同じ 2 地点に対して、**幹線を通る直線（400m）と生活道路の迂回（577m）**の
2 通りがあるグラフで確かめる。

| ケース | 目的 |
|---|---|
| 2 地点を繋ぐ経路を返す | 出発点と目的地を含むこと |
| **幹線を避けて生活道路を選ぶ** | **このアプリの価値そのもの。** 遠回りでも生活道路を選ぶ |
| **係数を平らにすると幹線を通る** | **係数が経路を変えている証拠。** 距離だけなら短い幹線が選ばれる |
| 遠回りのぶん距離は伸びる | 幹線回避の代償が出ていること |
| 同じノードなら距離 0 の経路を返す | |
| 繋がっていなければ null を返す | 川や線路で分断された先 |
| 知らないノードを指定したら null を返す | 例外を投げないこと |
| 距離とコストの両方を返す | 係数の効き具合を見るため |
| 幹線しか道が無ければ、やむを得ず通る | **禁止ではなく高コスト。** 分断された地形で到達できなくならないこと |
| 種別ごとの距離を返す | 経路の内訳を数字で見る |
| 距離の大きい順に並べる | |

**フィクスチャの注意:** 迂回路は東へふくらませてある。まっすぐ並べると
直線と同じ長さになり、係数を平らにしたときに引き分けになってしまう。

---

## lib/region.ts — 地図の表示範囲

`__tests__/region-test.ts`

`react-native-maps` に渡す Region を、中心と半径から作る。

| ケース | 目的 |
|---|---|
| 中心をそのまま返す | |
| 半径 500m ならおよそ 1km 四方になる | 緯度 1 度 ≒ 111km の換算 |
| 半径を倍にすると範囲も倍になる | 線形であること |
| 緯度が高いほど経度方向を広く取る | 経線は極に近づくほど間隔が狭まる |
| 赤道では緯度と経度の幅がほぼ等しい | 補正の基準点 |
| 東京では経度方向が緯度方向の約 1.2 倍になる | 実際に使う緯度での値 |
| 極端な緯度でも 0 除算にならない | `cos` が 0 に近づく場合 |

---

## 実測値との突き合わせ

自動テストでは通信しない。取得が正しいかは、開発画面に出る値を
下表と突き合わせて判断する（開発タイル: 武蔵小山 `6985/1781`）。

| 項目 | 実測値 |
|---|---|
| way | 1,806 |
| node | 6,391 |
| 交差点ノード | 2,767 |
| エッジ | 3,777 |
| 総延長 | 162.9 km |
| 孤立ノード | 0 |

数値がずれた場合の切り分け:

- **way / node がずれる** → タイル計算か、Overpass の抽出条件
- **way / node は合うがグラフがずれる** → 分割処理（`buildGraph`）
- **孤立ノードが 0 でない** → 隣接リストの構築漏れ
